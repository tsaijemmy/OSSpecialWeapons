演進過程拆解

目標： v$database

在 SQLPlus 裡： 需要 v\$database（有時為了防止某些環境誤判）。

在 Shell 腳本裡： 為了產生那條斜線，得寫成 \\。

在反引號指令替換中： 規則再次加倍。

具體運作邏輯如下：

\\\\：前兩條斜線變成一條文字斜線 \，後兩條斜線也變成一條文字斜線 \。

結果傳給 Shell 處理後剩下 \\$。

最後進到 SQLPlus 時，\\ 變成 \，最終 SQL 看到的就是 v\$database 或 v$database。
